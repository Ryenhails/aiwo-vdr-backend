__pycache__/
*.pyc
.env
.env.local

# Data — commit the source PDF and the embeddings index (small and
# expensive to rebuild). Skip the 180+ MB of page PNGs: a collaborator can
# regenerate them from the PDF with PyMuPDF in ~20 s on CPU (see
# INTEGRATION_GUIDE.md §7.3).
data/images/

# Transients
*.log
.ipynb_checkpoints/
